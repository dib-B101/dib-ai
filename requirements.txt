# AI 서버 실행에 필요한 것 (`uvicorn serve:app`).
#
#     pip install -r requirements.txt
#
# **임베딩 배치는 여기 없다.** torch·transformers 는 2GB 가 넘는데 서버는 쓰지
# 않는다 — 벡터를 만드는 것은 `scripts/embed_products.py` 이고, 서버는 DB 에 이미
# 들어 있는 벡터를 읽기만 한다. 배치를 돌릴 때만 requirements-batch.txt 를 깐다.
#
#     서버      이 파일                    탐지 · 검수 · 추천 API
#     배치      requirements-batch.txt     상품 임베딩 (GPU 권장)
#     개발      requirements-dev.txt       테스트 · 노트북 · 모델 학습

# --- API ---------------------------------------------------------------------
fastapi>=0.110
uvicorn[standard]>=0.27
python-dotenv>=1.0   # .env 로 키·모델 설정
pyyaml>=6.0          # 규칙·추천 설정 로더

# --- DB 조회 -----------------------------------------------------------------
# 탐지·추천의 PostgresProvider. 없으면 합성 데이터로만 동작한다.
psycopg[binary]>=3.1

# --- 이상거래 탐지 ML 트랙 ----------------------------------------------------
# 모델은 artifacts/ 에 들어 있다. 없으면 규칙 트랙만으로 뜬다.
lightgbm>=4.0
scikit-learn         # CalibratedClassifierCV 로 저장돼 있어 역직렬화에 필요
joblib               # 모델 로드
pandas               # 추론 입력을 DataFrame 으로 만든다 (predict.py)
numpy

# --- 상품 검수 ---------------------------------------------------------------
pyahocorasick>=2.0   # 금칙어 다중 패턴 매칭
pillow>=10.0         # 이미지 리사이즈 (토큰 절감)
anthropic>=0.40      # 2차 AI 검수 — Claude
openai>=1.40         # 2차 AI 검수 — GPT (둘 중 하나만 있어도 동작)
